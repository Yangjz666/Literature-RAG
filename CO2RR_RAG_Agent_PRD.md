# CO2RR 文献 RAG 阅读与实验信息提取 Agent 需求文档

## 1. 项目名称

**CO2RR 文献 RAG 阅读与实验信息提取 Agent**

---

## 2. 项目目标

开发一个能够读取指定文件夹内所有文献 PDF 的本地 RAG Agent。

该 Agent 可以根据用户的自然语言问题，自动在本地文献库中检索相关内容，并输出结构化答案。

例如用户提问：

> AgNPs 的合成方法有哪些？

Agent 应该自动搜索文件夹中的所有相关文献，找到涉及 AgNPs 合成的内容，并生成结构化表格。表格中需要包含文献名称、催化剂名称、实验方法、原料、反应条件、后处理条件和原文证据。

本项目的核心目标是：

> **让 Agent 输出的每一条关键信息都有原文句子作为证据，避免瞎编。**

---

## 3. 背景与价值

用户是一名材料方向研究生，课题方向是电催化 CO2 还原反应 CO2RR。日常科研工作中需要大量阅读文献，整理催化剂合成方法、测试条件、性能数据和机理解释。

目前人工阅读文献存在以下问题：

1. 文献数量多，手动查找实验方法效率低；
2. 不同文献的实验条件分散在正文、实验部分和 Supporting Information 中；
3. 手动整理表格容易遗漏关键信息；
4. 普通 AI 总结容易产生幻觉，无法确认信息来源；
5. 后续写论文或设计实验时，需要快速对比不同文献的实验方案。

因此，需要一个专门面向 CO2RR 文献的 RAG Agent，帮助用户从本地文献库中快速提取可信信息。

---

## 4. 核心使用场景

### 4.1 查询某类催化剂的合成方法

用户提问：

> 帮我总结文献中 AgNPs 的合成方法。

Agent 需要输出：

| 文献名称 | 催化剂 | 合成路线 | 原料 | 具体步骤 | 后处理 | 原文证据 |
|---|---|---|---|---|---|---|

要求：

- 自动检索所有包含 AgNPs、Ag NPs、silver nanoparticles、Ag nanoparticles 等关键词的文献；
- 提取实验部分中的合成方法；
- 区分原料、溶剂、还原剂、反应温度、反应时间、洗涤方式、干燥条件；
- 每一条实验条件后面都需要附带原文句子作为证据。

---

### 4.2 查询某种添加剂或分子的作用机理

用户提问：

> 咪唑类添加剂在 CO2RR 中的作用是什么？

Agent 需要输出：

| 文献名称 | 添加剂名称 | 催化剂体系 | 主要作用 | 机理解释 | 原文证据 |
|---|---|---|---|---|---|

要求：

- 自动检索 imidazolium、ionic liquid、CO2 enrichment、HER suppression、stabilize intermediate 等相关内容；
- 总结其对 CO2 富集、HER 抑制、中间体稳定、电双层调控等作用；
- 输出时区分“文献明确结论”和“Agent 推测分析”。

---

### 4.3 对比不同文献的实验条件

用户提问：

> 对比文献中 Ag 基催化剂用于 CO2RR 的测试条件。

Agent 需要输出：

| 文献名称 | 催化剂 | 电解池类型 | 电解液 | CO2 流速 | 电位/电流密度 | 主要产物 | FE | 原文证据 |
|---|---|---|---|---|---|---|---|---|

要求：

- 能从正文、实验部分、图注、Supporting Information 中提取测试条件；
- 能识别 H-cell、flow cell、MEA 等测试体系；
- 能提取电解液浓度，例如 0.1 M KHCO3、1 M KOH、0.5 M KHCO3；
- 能提取 CO2 流速、外加电位、电流密度、FE、产物分布等信息。

---

### 4.4 根据问题生成 Markdown 文献笔记

用户提问：

> 帮我整理这几篇关于 AgNPs 的文献笔记。

Agent 输出 Markdown 文件，推荐结构如下：

```md
# AgNPs 文献整理

## 1. 总体结论

## 2. 各文献实验方案

## 3. 合成方法对比表

## 4. 测试条件对比表

## 5. 可借鉴实验路线

## 6. 原文证据摘录

## 7. Agent 不确定的信息
```

---

## 5. 输入要求

### 5.1 文献输入方式

用户指定一个本地文件夹路径，例如：

```bash
D:/CO2RR_PDFs/AgNPs
```

Agent 自动读取该文件夹中的所有文献文件。

支持格式：

- PDF 正文；
- PDF Supporting Information；
- Markdown 文献笔记；
- TXT 文本；
- 后续可扩展 Word、HTML、网页链接。
自然语言处理在化学领域中的应用

通过将AI应用于分析和提取大量科学文献、专利和化学数据库中的有价值信息，自然语言处理将非结构化文本转化为结构化数据，从而更高效地解释数据。这可以揭示科学文献中传统阅读方法无法发现的隐藏联系和模式，帮助研究人员专注于实验。在制药领域，自然语言处理有助于通过提取生物活性化合物、靶点和治疗效果等信息来推动药物发现和开发。
**文献标识规则：**

输出表格中"文献名称"列的显示优先级：
1. 从 PDF 元数据或首页提取的论文标题（如能成功解析）；
2. 作者姓氏 + 年份（如 `Zhang_2023`，从元数据提取）；
3. 回退到文件名（如 `paper_01.pdf`）。

---

### 5.2 用户提问方式

用户可以直接用自然语言提问，例如：

```text
AgNPs 的合成方法有哪些？
```

```text
这几篇文献中 Ag 催化剂的 CO2RR 测试条件是什么？
```

```text
哪些文献提到了咪唑类添加剂可以抑制 HER？
```

```text
帮我找出所有使用 NaBH4 还原法合成 AgNPs 的文献。
```

---

## 6. 输出要求

### 6.1 输出格式

默认输出为 Markdown 格式，方便复制到 Obsidian、论文笔记或实验记录中。

主要输出形式包括：

1. 总结性文字；
2. 对比表格；
3. 原文证据摘录；
4. 不确定信息说明；
5. 可借鉴实验路线（仅基于原文内容，不生成新方案）。

**结果自动保存：**

每次查询结果自动保存到 `output/` 文件夹，文件名格式：

```text
output/20250101_103000_AgNPs合成方法.md
```

用户也可以在界面中手动指定保存路径。

---

### 6.2 表格输出要求

以“AgNPs 合成方法”为例，推荐输出表格如下：

| 编号 | 文献名称 | 催化剂名称 | 合成方法 | 原料 | 关键参数 | 后处理 | 原文证据 | 页码/位置 |
|---|---|---|---|---|---|---|---|---|
| 1 | xxx.pdf | Ag NPs | NaBH4 还原法 | AgNO3, sodium citrate, NaBH4 | 室温搅拌 2 h | 离心、洗涤、60 °C 真空干燥 | “The Ag NPs were prepared...” | Experimental section |

---

## 7. 原文验证要求

这是本 Agent 最重要的功能。

### 7.1 每条关键信息必须有证据

凡是涉及以下内容，必须提供原文证据：

- 催化剂名称；
- 原料；
- 合成步骤；
- 反应温度；
- 反应时间；
- 电解液；
- 电解池类型；
- 电位；
- 电流密度；
- FE；
- 产物；
- 机理结论。

---

### 7.2 原文证据格式

推荐格式：

```md
**提取信息：** AgNPs 由 AgNO3、sodium citrate 和 NaBH4 制备。  
**原文证据：** “20 mL aqueous solution containing 141.6 mmol L-1 sodium citrate was titrated with 20 mL aqueous solution containing 20 mmol L-1 AgNO3...”  
**来源：** Paper_01.pdf，Experimental section，第 X 页。
```

---

### 7.3 区分“原文事实”和“Agent 推测”

Agent 输出时必须明确区分：

```md
### 文献明确说明

该文献明确指出 AgNPs 通过 NaBH4 还原 AgNO3 获得。

### Agent 分析

根据实验条件推测，该方法属于典型的化学还原法，sodium citrate 可能起到稳定剂作用。但如果文中没有明确说明，则不能写成文献结论。
```

禁止把推测内容伪装成原文结论。

---

## 8. 功能模块设计

### 8.1 文献导入模块

功能：

- 读取指定文件夹；
- 自动识别 PDF 文件；
- 提取正文文本；
- 尝试提取页码、标题、DOI、作者、期刊信息；
- 区分正文和 Supporting Information。

---

### 8.2 文献切分模块

功能：

- 按标题、小标题、段落进行切分；
- 保留 chunk 的来源信息；
- 每个 chunk 需要包含：
  - 文献名称；
  - 页码；
  - 章节标题；
  - 原文内容；
  - DOI 或文件名；
  - 是否来自 Supporting Information。

推荐切分方式：

```text
文献 → 章节 → 小节 → 段落 chunk
```

不要只按固定字符数粗暴切分，否则容易把实验步骤切断。

**父子 chunk 策略（推荐）：**

为了同时满足"检索精度"和"证据粒度"两个需求，采用父子 chunk 双层索引：

- 子 chunk（用于索引和检索）：单个句子或 2-3 句话，约 100-200 字符，粒度细，检索精准；
- 父 chunk（用于 LLM 提取）：整个段落，约 500-1000 字符，上下文完整，提取准确；
- 检索时用子 chunk 定位，送入 LLM 时扩展为父 chunk。

这样 LLM 提取的 `evidence_sentence` 可以精确对应到子 chunk 原文，便于证据校验。

---

### 8.3 向量检索模块

功能：

- 对文献 chunk 建立向量索引；
- 用户提问后，自动检索最相关的原文片段；
- 支持关键词检索 + 向量检索混合模式。

推荐支持：

- 向量检索：适合语义相似问题；
- BM25 关键词检索：适合查找 AgNO3、NaBH4、KHCO3 等具体词；
- 混合检索：提高召回率。

---

### 8.4 信息抽取模块

功能：

从检索到的原文中提取结构化信息。

例如针对合成方法，抽取字段包括：

```json
{
  "paper_name": "",
  "catalyst": "",
  "method_type": "",
  "precursors": [],
  "solvent": "",
  "temperature": "",
  "time": "",
  "post_treatment": "",
  "evidence_sentence": "",
  "page": ""
}
```

---

### 8.5 证据校验模块

功能：

- 检查每一个结论是否有原文证据；
- 没有证据的内容必须标记为”不确定”；
- 如果原文没有明确说明，不能强行补充；
- 输出前检查表格中的每一行是否至少有一条 evidence。

**证据真实性校验算法：**

LLM 提取的 `evidence_sentence` 必须通过以下校验才能标记为”高可信度”：

1. 精确匹配：在来源 chunk 原文中能找到完全相同的字符串；
2. 模糊匹配（精确匹配失败时）：使用 difflib 计算相似度 ≥ 0.85，标记为”中可信度”；
3. 两者均失败：标记为”低可信度 - 证据未能在原文中验证”，不允许作为结论输出。

```python
# 校验逻辑示意
def verify_evidence(evidence_sentence, source_chunk_text):
    if evidence_sentence in source_chunk_text:
        return “高”, “精确匹配”
    ratio = difflib.SequenceMatcher(None, evidence_sentence, source_chunk_text).ratio()
    if ratio >= 0.85:
        return “中”, f”模糊匹配 {ratio:.2f}”
    return “低”, “证据未能在原文中验证”
```

---

### 8.6 答案生成模块

功能：

- 根据用户问题生成最终答案；
- 输出 Markdown；
- 优先输出表格；
- 保留原文证据；
- 在最后列出不确定项；
- 自动将结果保存到输出文件夹，文件名格式：`YYYYMMDD_HHMMSS_查询关键词.md`。

---

### 8.7 索引管理模块（V1）

功能：

- 向量索引持久化到本地磁盘（Chroma 默认支持）；
- 启动时检测文件夹内容是否变化（基于文件名+修改时间哈希）；
- 如果文件夹有新增文件，只对新文件做增量索引，不重建全量；
- 如果文件被删除，从索引中移除对应 chunk；
- 提供手动强制重建索引的命令。

索引状态文件示例（`index_manifest.json`）：

```json
{
  "last_updated": "2025-01-01T10:00:00",
  "files": {
    "paper_01.pdf": {"mtime": 1234567890, "chunk_count": 42},
    "paper_02.pdf": {"mtime": 1234567891, "chunk_count": 38}
  }
}
```

---

### 8.8 PDF 异常处理模块（V1）

**文件类型识别：**

- 文字型 PDF：直接用 PyMuPDF 提取文本；
- 扫描版 / 图片型 PDF：检测到文本提取为空时，自动降级到 OCR（pytesseract）；
- 加密 PDF：跳过并在日志中记录，提示用户手动解密；
- 解析超时（>60 秒）：跳过并记录，不中断整体流程。

**Supporting Information 关联规则：**

SI 文件识别优先级：
1. 文件名包含 `SI`、`Supporting`、`Supplementary`、`ESI`（不区分大小写）；
2. 用户可在文件夹中创建 `si/` 子目录，其中所有文件视为 SI；
3. 无法自动识别时，SI 文件与主文献共享文件名前缀（如 `paper_01_SI.pdf` 对应 `paper_01.pdf`）。

SI chunk 在索引中标记 `is_si: true`，检索时默认包含 SI 内容，输出时注明来源。

**文献去重规则：**

- 优先基于 DOI 去重（从 PDF 元数据或首页文本提取）；
- 无 DOI 时，基于文件内容前 500 字符的哈希去重；
- 发现重复时保留文件名字典序靠前的版本，并在日志中提示用户。

---

## 9. 推荐 Agent 工作流程

用户提问：

```text
AgNPs 的合成方法有哪些？
```

Agent 执行流程：

```text
1. 读取用户指定文件夹
2. 解析所有 PDF 文献
3. 建立文献索引
4. 根据问题生成多个检索关键词
   - AgNPs
   - Ag nanoparticles
   - silver nanoparticles
   - Ag NPs
   - synthesis
   - preparation
   - NaBH4
   - AgNO3
5. 检索相关原文片段
6. 提取实验条件
7. 生成结构化表格
8. 为每条信息匹配原文证据
9. 检查是否存在无证据结论
10. 输出 Markdown 结果
```

---

## 10. 检索关键词扩展要求

Agent 需要自动识别同义词。

例如用户问：

> AgNPs 的合成方法

Agent 应该自动扩展为：

```text
AgNPs
Ag NPs
Ag nanoparticles
silver nanoparticles
silver NPs
Ag catalyst
Ag nanocatalyst
preparation
synthesis
fabrication
```

如果用户问：

> 咪唑添加剂

Agent 应该自动扩展为：

```text
imidazole
imidazolium
ionic liquid
IL
APMImBr
CO2 enrichment
HER suppression
electrolyte additive
cationic additive
```

---

## 11. 输出可信度要求

Agent 输出每条内容时建议给出可信度等级：

| 等级 | 含义 | 判定条件 |
|---|---|---|
| 高 | 原文明确给出，且有完整句子证据 | evidence_sentence 精确匹配原文，检索分数 ≥ 0.85 |
| 中 | 原文部分给出，需要结合上下文理解 | evidence_sentence 模糊匹配相似度 ≥ 0.85，或检索分数 0.65-0.85 |
| 低 | 检索到相关信息，但不能完全确定 | 检索分数 0.5-0.65，或 evidence_sentence 无法在原文中验证 |
| 不确定 | 没有找到足够证据，不应作为结论 | 检索分数 < 0.5，或无任何相关 chunk |

示例：

```md
可信度：高  
原因：原文实验部分明确给出了 AgNO3、sodium citrate、NaBH4 的用量和反应时间。
证据校验：精确匹配，来源 paper_01.pdf 第 3 页 Experimental section。
```

---

## 12. 防止幻觉的规则

Agent 必须遵守以下规则：

1. 没有原文证据，不允许编造实验条件；
2. 不允许自动补全温度、时间、浓度；
3. 不允许把常识写成文献结论；
4. 如果只在 Supporting Information 中找到信息，需要标注“来源于 Supporting Information”；
5. 如果文献中没有写明某个参数，应写“未明确说明”；
6. 如果多个文献条件不同，不能合并成一个条件；
7. 如果 Agent 进行推测，必须单独放在“Agent 分析”部分。

---

## 13. MVP 最小可行版本

第一阶段建议不要做太复杂，只做最核心功能。

### 13.1 V1 目标

实现：

1. 读取指定文件夹 PDF（含 OCR 降级处理）；
2. 提取 PDF 文本，自动识别并关联 SI 文件；
3. 建立本地向量数据库（Chroma，持久化到磁盘）；
4. 支持增量索引更新，新增文件无需重建全量索引；
5. 根据用户问题检索相关段落（BM25 + 向量混合，RRF 融合）；
6. 输出 Markdown 表格；
7. 每条结果附带原文证据，并通过字符串回查校验；
8. 标注文献名称和页码；
9. 查询结果自动保存到 `output/` 文件夹；
10. Streamlit 界面，显示索引进度和查询状态。

### 13.2 V1 性能目标

| 指标 | 目标值 |
|---|---|
| 文献库规模 | 支持 ≤ 100 篇 PDF，总页数 ≤ 3000 页 |
| 索引建立时间 | 100 篇文献全量建索引 ≤ 10 分钟 |
| 单次查询响应时间 | ≤ 60 秒（含 LLM 提取） |
| 增量索引时间 | 新增 1 篇文献 ≤ 30 秒 |

### 13.3 V1 暂时不做

暂时不做：

- 自动画图；
- 自动写论文；
- 自动联网查文献；
- 自动分析图片中的数据；
- 自动判断机理对错；
- 复杂多 Agent 系统；
- FastAPI 服务化；
- 查询历史管理界面；
- 多轮对话（V1 每次查询独立，不保留上下文）。

第一阶段先把”能从本地文献中准确找实验方法”做好。

---

## 14. 后续扩展功能

### 14.1 V2：多 Agent 协作

可以拆成几个子 Agent：

| Agent | 作用 |
|---|---|
| Search Agent | 负责检索相关文献片段 |
| Extract Agent | 负责提取实验条件 |
| Compare Agent | 负责对比不同文献 |
| Evidence Agent | 负责检查原文证据 |
| Report Agent | 负责生成 Markdown 报告 |

---

### 14.2 V3：CO2RR 专业字段增强

增加专门针对 CO2RR 的字段：

| 类别 | 字段 |
|---|---|
| 催化剂 | 材料名称、形貌、负载量、基底 |
| 合成 | 原料、溶剂、温度、时间、后处理 |
| 电解池 | H-cell、flow cell、MEA |
| 电解液 | KHCO3、KOH、H2SO4、KCl |
| 测试 | CO2 流速、电位、电流密度 |
| 性能 | FE、partial current density、SPCE、稳定性 |
| 机理 | CO2 富集、HER 抑制、中间体稳定、电双层调控 |

---

## 15. 推荐技术路线

适合 AI 小白的技术路线：

```text
PDF 文件夹
→ PDF 解析（含 OCR 降级）
→ 文本切分（父子 chunk 策略）
→ 向量数据库（持久化到本地）
→ 混合检索（BM25 + 向量，RRF 融合）
→ LLM 信息抽取
→ 证据校验（字符串回查原文）
→ Markdown 输出 + 自动保存
```

推荐技术栈：

| 功能 | 推荐工具 | 版本归属 |
|---|---|---|
| PDF 解析 | PyMuPDF（首选）/ marker（扫描版降级） | V1 |
| OCR 降级 | pytesseract / surya | V1 |
| 文本切分 | LlamaIndex（父子 chunk） | V1 |
| 向量数据库 | Chroma（本地持久化） | V1 |
| 关键词检索 | BM25Okapi | V1 |
| 混合检索融合 | Reciprocal Rank Fusion (RRF) | V1 |
| LLM | Ollama + qwen2.5（本地）/ Claude API（云端可选） | V1 |
| 本地界面 | Streamlit | V1 |
| API 服务 | FastAPI | V2 |
| 输出格式 | Markdown | V1 |

**LLM 选型说明：**

V1 推荐优先使用本地 LLM（Ollama），避免文献内容上传到第三方服务器。如果本地算力不足，可使用 Claude API，但需注意文献内容会离开本地环境。

- 本地方案：Ollama + qwen2.5:14b 或 llama3.1:8b，无网络依赖，无 API 费用
- 云端方案：Claude claude-sonnet-4-6 API，效果更好，但需要网络和 API Key，有费用

**混合检索融合策略：**

使用 Reciprocal Rank Fusion (RRF) 合并 BM25 和向量检索结果：

```python
# RRF 公式：score = Σ 1/(k + rank_i)，k=60
rrf_score = 1/(60 + bm25_rank) + 1/(60 + vector_rank)
```

取融合后 top-10 chunk 送入 LLM 提取。

---

## 16. 给 Codex / Claude 的最终需求描述

可以直接复制给 Codex 或 Claude：

```md
我想开发一个面向 CO2RR 文献阅读的本地 RAG Agent。

该 Agent 需要读取用户指定文件夹中的所有 PDF 文献，包括正文和 Supporting Information。用户可以用自然语言提问，例如“AgNPs 的合成方法有哪些？”、“哪些文献使用 NaBH4 还原法制备 AgNPs？”、“咪唑类添加剂在 CO2RR 中的作用是什么？”

Agent 需要自动检索本地文献库，找到与问题相关的原文片段，并从中提取结构化信息。以 AgNPs 合成方法为例，Agent 需要输出表格，字段包括：文献名称、催化剂名称、合成方法、原料、溶剂、反应温度、反应时间、后处理条件、原文证据、页码或章节位置。

Agent 的核心要求是防止幻觉。每一条关键信息都必须附带原文证据，例如原文句子、文献名称、页码或章节。如果原文没有明确说明某个参数，必须写“未明确说明”，不能根据常识自动补全。如果 Agent 做出推测，必须单独放在“Agent 分析”部分，不能伪装成文献结论。

第一阶段只实现最小可行版本：读取本地 PDF 文件夹、解析文本、建立向量索引、根据用户问题检索相关内容、提取实验条件、生成 Markdown 表格，并提供原文证据。暂时不需要联网查文献、自动画图或自动写论文。

输出格式默认使用 Markdown，方便复制到 Obsidian 或实验记录中。
```

---

## 17. 项目一句话定位

**这是一个面向 CO2RR 文献的本地 RAG 实验信息提取 Agent，重点不是泛泛总结文献，而是从文献原文中提取可验证、可复查、可用于实验设计的结构化信息。**

---

## 18. 用户体验与错误处理要求

### 18.1 索引建立进度反馈

用户启动索引时，界面需要显示：

```text
正在处理文献 (12/47)：Zhang_2023_AgNPs.pdf
预计剩余时间：约 3 分钟
```

索引完成后显示摘要：

```text
索引完成：47 篇文献，共 1823 个 chunk
跳过：2 篇（加密 PDF，见 index.log）
重复：1 篇（与 paper_03.pdf 内容相同，已忽略）
```

### 18.2 查询状态反馈

查询过程中显示当前步骤：

```text
[1/4] 检索相关段落...
[2/4] 提取实验条件...
[3/4] 校验原文证据...
[4/4] 生成 Markdown 输出...
```

### 18.3 异常情况用户提示

| 异常情况 | 用户提示 |
|---|---|
| 检索结果为空 | "在当前文献库中未找到相关内容，请尝试换用其他关键词，或检查文献是否已正确导入。" |
| PDF 解析失败 | "以下文件解析失败，已跳过：[文件名]。可能是加密 PDF 或扫描版，请手动检查。" |
| LLM 调用失败 | "LLM 服务暂时不可用，请检查 Ollama 是否已启动，或 API Key 是否有效。" |
| 证据校验失败 | 在输出中标注"⚠️ 证据未能在原文中验证，请人工核查"，不阻断输出。 |
| 文献库为空 | "当前文件夹中没有找到支持的文献文件，请确认路径是否正确。" |

### 18.4 多轮对话（V2）

V1 每次查询独立，不保留上下文。

V2 支持基础多轮对话：
- 保留当前会话的"工作文献集"（用户本次查询涉及的文献列表）；
- 支持追问，例如"刚才那篇文献的 FE 数据是多少？"；
- 会话结束或用户切换文件夹时重置上下文。

### 18.5 查询历史（V2）

V2 支持查询历史：
- 自动记录每次查询的问题、时间、涉及文献、输出文件路径；
- 用户可在界面中浏览和重新打开历史查询结果；
- 历史记录保存在 `output/history.json`。
